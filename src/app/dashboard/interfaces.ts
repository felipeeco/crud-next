export interface ActivityInterface {
  title: string;
  cost: number;
  percentage: number;
  description: string;
}

export interface Menu {
  title: string;
  icon: React.ReactNode;
  href: string;
}